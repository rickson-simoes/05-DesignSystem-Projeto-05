{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel --no-daemon",
    "build": "turbo run build"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.1",
    "turbo": "^1.9.3"
  }
}
